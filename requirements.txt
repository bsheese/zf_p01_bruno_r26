# Python dependencies for the zebrafish novel-tank reliability reanalysis.
# Pinned to the versions used to produce the results (Python 3.12).
#   pip install -r requirements.txt
#
# The manuscript build additionally needs system tools, not Python packages:
#   pandoc (>=3), a TeX install with xelatex, and the DejaVu Serif font.

pandas==3.0.2
numpy==2.4.4
scipy==1.17.1
statsmodels==0.14.6
pingouin==0.6.1
scikit-learn==1.8.0
matplotlib==3.10.8
openpyxl==3.1.5
